/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["newsghana.com.gh", "www.globalgiving.org", "agrierp.com"],
  },
};

export default nextConfig;
